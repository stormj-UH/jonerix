[package]
name = "unbound"
version = "1.22.0"
license = "BSD-3-Clause"
pre_bootstrap = false
description = "Validating, recursive, and caching DNS resolver"

[build]
system = "custom"
build = ""
install = ""

[depends]
runtime = ["musl", "openssl"]
build = []
