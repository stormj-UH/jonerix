[package]
name = "openrc"
version = "0.54"
license = "BSD-2-Clause"
pre_bootstrap = false
description = "Dependency-based init system for Unix-like systems"

[build]
system = "custom"
build = ""
install = ""

[depends]
runtime = ["musl"]
build = []
