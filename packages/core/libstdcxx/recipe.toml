[package]
name = "libstdcxx"
version = "15.2.0"
license = "GPL-3.0 WITH GCC-exception-3.1"
pre_bootstrap = false
description = "GCC C++ standard library runtime (libstdc++.so)"

[build]
build = ""
install = ""

[depends]
runtime = ["musl", "libgcc"]
build = []
