[package]
name = "bsdtar"
version = "3.8.5"
license = "Apache-2.0"
pre_bootstrap = false
description = "BSD tar from libarchive (handles symlinks correctly)"

[build]
system = "custom"
build = ""
install = ""

[depends]
runtime = ["musl"]
build = []
