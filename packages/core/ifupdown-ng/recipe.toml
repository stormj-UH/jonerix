[package]
name = "ifupdown-ng"
version = "0.12.1"
license = "ISC"
pre_bootstrap = true
description = "Next-generation network interface configuration tool"

[build]
system = "custom"
build = """
set -e
apk add --no-cache ifupdown-ng
for pkg in ifupdown-ng; do
  apk info --contents "$pkg" 2>/dev/null | tail -n +2 | grep -v '/\$' | while IFS= read -r f; do
    f="${f%/}"
    src="/${f}"
    [ -e "$src" ] || continue
    dst="${DESTDIR}/${f}"
    mkdir -p "$(dirname "$dst")"
    cp -aP "$src" "$dst" 2>/dev/null || true
  done
done
if [ -d "${DESTDIR}/usr" ]; then
  cp -a "${DESTDIR}/usr/." "${DESTDIR}/"
  rm -rf "${DESTDIR}/usr"
fi
"""
install = ""

[depends]
runtime = ["musl"]
build = []
