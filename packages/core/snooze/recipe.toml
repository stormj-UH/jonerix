[package]
name = "snooze"
version = "0.5"
license = "CC0"
pre_bootstrap = false
description = "Lightweight cron alternative for running a command at a specific time"

[build]
system = "custom"
build = ""
install = ""

[depends]
runtime = ["musl"]
build = []
