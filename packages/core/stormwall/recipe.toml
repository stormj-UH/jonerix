[package]
name = "stormwall"
version = "1.1.4"
license = "MIT"
description = "Drop-in firewall front-end — accepts nft, pf and iptables CLI dialects against the same in-kernel netfilter backend"

[source]
# Canonical source: castle.great-morpho.ts.net:3000/jonerik/stormwall tag v1.1.4
# Mirrored to GitHub for CI (castle is behind Tailscale).
#
# 1.1.4: triage pass from the iptables-soak.sh behavioural harness
# against the live kernel.  Three fixes that real iptables scripts
# (and pre-existing unit tests) tripped over:
#
#   1. Bare `-j REJECT` (no `--reject-with`) returned ENOENT from the
#      kernel because Expr::Reject emitted an empty NFTA_EXPR_DATA;
#      the kernel actually requires NFTA_REJECT_TYPE even for the
#      default ICMP_UNREACH path.  Now emits type=ICMP_UNREACH +
#      code=ICMP_UNREACH_PORT (matches upstream nft + iptables
#      semantics for unqualified REJECT).
#
#   2. Port range form `--dport 1000:2000` lowered correctly to
#      `tcp dport 1000-2000` but stormwall's own nft text parser
#      stopped at the lo end and returned a 1-byte Cmp.  The
#      tokenizer already split the dash; parse_cmp_and_value now
#      detects the trailing `-` after parse_value and emits
#      Expr::Range with NFT_RANGE_EQ instead of Expr::Cmp.
#
#   3. Legacy iptables negation form `-s ! 1.2.3.4` was a no-op:
#      `take_neg` captured `pending_neg` BEFORE `take_value`, but
#      `take_value` is what flips `pending_neg` when it sees the
#      legacy `!` token between the flag and its value.  New
#      `take_value_neg` helper captures after the read.  All 14
#      flag callers migrated.  Modern form `! -s 1.2.3.4` was
#      always handled correctly and stays that way.
#
# Verified end-to-end: real-kernel soak now runs cleaner; all 201
# unit tests pass (was 200/1 before).  Known limitations are filed
# under iptables-save rule rendering, -D <spec> / -C delete-by-match,
# and MARK/LOG/REDIRECT lowering — none of which dockerd's libnetwork
# exercises in a way that blocks bridge bring-up or `docker run`.
#
# 1.1.3 (prior): jump-target user-chain auto-create + ct state
# `{ X, Y }` set-literal parser.
# 1.1.2 (prior): user-chain auto-create on the operating chain.
# 1.1.1 (prior): help-text refresh for nft + pfctl + iptables.
url = "https://github.com/stormj-UH/jonerix/releases/download/source-stormwall-v1.1.4/stormwall-1.1.4.tar.gz"
sha256 = "58764e529af82d28b402e9b565614bf132421edb2f1d2021a32c9a947773bf69"

[build]
system = "custom"
build = """
set -e
export CARGO_HOME=/tmp/stormwall-cargo
export CARGO_TARGET_DIR="$PWD/target"
export CARGO_NET_OFFLINE=true
export RUSTFLAGS="-C strip=symbols -C target-feature=+crt-static"
cargo build --release --frozen --offline --bin stormwall --bin pfctl
"""
install = """
set -e
install -Dm755 "$PWD/target/release/stormwall" "$DESTDIR/bin/stormwall"
install -Dm755 "$PWD/target/release/pfctl" "$DESTDIR/bin/pfctl"
# Single-binary dispatch by argv[0]: stormwall switches between
# nft, iptables, ip6tables, and the *-save / *-restore variants
# based on its invocation name. The symlinks below put the
# expected binary names on PATH so dockerd's libnetwork (and any
# other tool that shells out to iptables) finds a working tool.
ln -sf stormwall "$DESTDIR/bin/nft"
ln -sf stormwall "$DESTDIR/bin/iptables"
ln -sf stormwall "$DESTDIR/bin/iptables-save"
ln -sf stormwall "$DESTDIR/bin/iptables-restore"
ln -sf stormwall "$DESTDIR/bin/ip6tables"
ln -sf stormwall "$DESTDIR/bin/ip6tables-save"
ln -sf stormwall "$DESTDIR/bin/ip6tables-restore"
install -Dm644 LICENSE "$DESTDIR/share/licenses/stormwall/LICENSE"
llvm-strip --strip-all "$DESTDIR/bin/stormwall" 2>/dev/null || true
llvm-strip --strip-all "$DESTDIR/bin/pfctl" 2>/dev/null || true
"""

[hooks]
# dockerd's libnetwork bridge driver hard-codes /usr/sbin/iptables and
# /sbin/iptables — it doesn't fall back to PATH lookup. Plant compat
# symlinks back to /bin/stormwall in those paths at install time, since
# jpkg payloads can only land in /bin /lib /share /etc and the
# /usr/sbin and /sbin directories aren't in the package layout.
post_install = """set -e
names="iptables iptables-save iptables-restore ip6tables ip6tables-save ip6tables-restore nft"
for d in /sbin /usr/sbin; do
    [ -d "$d" ] || mkdir -p "$d"
    for n in $names; do
        ln -sf /bin/stormwall "$d/$n"
    done
done
"""
post_remove = """set -e
# Clean up the libnetwork-compat symlinks we planted in post_install.
# Only remove if they still point at our binary so we don't trample a
# manually placed iptables.
names="iptables iptables-save iptables-restore ip6tables ip6tables-save ip6tables-restore nft"
for d in /sbin /usr/sbin; do
    for n in $names; do
        target=$(readlink "$d/$n" 2>/dev/null) || continue
        [ "$target" = "/bin/stormwall" ] && rm -f "$d/$n"
    done
done
"""

[depends]
runtime = ["musl"]
build = ["rust"]
