[package]
name = "cmake"
version = "4.1.0"
license = "BSD-3-Clause"
description = "Cross-platform build system generator"

[source]
url = "https://github.com/Kitware/CMake/releases/download/v4.1.0/cmake-4.1.0.tar.gz"
sha256 = "81ee8170028865581a8e10eaf055afb620fa4baa0beb6387241241a975033508"

[build]
system = "custom"
configure = """${BOOTSTRAP_CMAKE:-cmake} -S . -B build -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_C_COMPILER=clang \
  -DCMAKE_CXX_COMPILER=clang++ \
  -DCMAKE_INSTALL_PREFIX=/ \
  -DCMAKE_INSTALL_LIBDIR=lib \
  -DCMAKE_USE_OPENSSL=OFF \
  -DLIBMD_LIBRARY=md \
  -DBUILD_CursesDialog=OFF \
  "-DCMAKE_C_FLAGS_INIT=--rtlib=compiler-rt -fuse-ld=lld" \
  "-DCMAKE_CXX_FLAGS_INIT=--rtlib=compiler-rt --unwindlib=libunwind -fuse-ld=lld -stdlib=libc++" \
  "-DCMAKE_EXE_LINKER_FLAGS_INIT=--rtlib=compiler-rt --unwindlib=libunwind -fuse-ld=lld" \
  "-DCMAKE_SHARED_LINKER_FLAGS_INIT=--rtlib=compiler-rt --unwindlib=libunwind -fuse-ld=lld" \
  -DBUILD_TESTING=OFF"""
build = "samu -C build -j$(nproc)"
install = """DESTDIR=$DESTDIR samu -C build install
if [ -d "$DESTDIR/usr" ]; then cp -a "$DESTDIR/usr/." "$DESTDIR/" && rm -rf "$DESTDIR/usr"; fi
if [ -d "$DESTDIR/lib64" ]; then cp -a "$DESTDIR/lib64/." "$DESTDIR/lib/" && rm -rf "$DESTDIR/lib64"; fi
python3 "$RECIPE_DIR/../../../scripts/normalize-lib64.py" "$DESTDIR"
if rg -n --text '/lib64' "$DESTDIR/bin" "$DESTDIR/share" 2>/dev/null; then
  echo "FATAL: cmake install still contains /lib64 references"
  exit 1
fi
find $DESTDIR/bin -type f -exec llvm-strip --strip-all {} \; 2>/dev/null || true"""

[depends]
runtime = ["musl"]
build = ["clang", "python3", "samurai"]
