[package]
name = "go"
version = "1.26.1"
license = "BSD-3-Clause"
description = "Go programming language compiler and tools"

# No [source] section: the build step downloads the arch-appropriate prebuilt
# toolchain directly from go.dev and verifies its sha256.

[build]
system = "custom"
build = ""
install = ""

[depends]
runtime = ["musl"]
build = []
