[package]
name = "jmake"
version = "1.0.14"
license = "MIT"
description = "Clean-room drop-in replacement for GNU Make, written in Rust"
# 1.0.14: fix parallel-scheduler deadlock that hung the builder
# forever when a CC fails with -j>1 and -k is not set. has_work()
# used to report the ready_queue as live work even after draining
# began — so main blocked on recv_result() while every worker sat
# idle. Reproduced in jonerix:builder-arm64 against wpa_supplicant
# with -j4 on 2026-04-20 via JMAKE_DEBUG_DEADLOCK. Also imports the
# 1.0.13 src tree onto Forgejo so the GNU Make 4.4.1 test suite
# hard-gate actually runs for every future jmake release.
#
# 1.0.13: populate $<, $^, $+, $? in expand_include_recipe_lines so
# recipes fired via `-include X.mk` (pending_includes rebuild path)
# get the same automatic variables GNU make sets. Previously only $@
# and $* were set, which made rules like:
#     uncommon.mk: $(srcdir)/common.mk
#             sed '...' $< > $@
# silently produce 0-byte files (sed reads stdin, redirects to $@).
# Ruby 3.4's build hit this exactly.

[source]
url = "https://cache.invalid/jmake-1.0.14.tar"
sha256 = "1769711ed8ebc024f3949decb592163c11772ff7eae63a82059e4b5d7441b4d9"

[build]
system = "custom"
# Rust 1.94.1-r2 uses llvm-libunwind=system, so library/unwind emits
# -lunwind instead of -lgcc_s. No cc-wrapper or gnu-compat dep needed.
# Source /etc/profile.d/cargo.sh so CARGO_HTTP_CAINFO is set (cargo's
# bundled libcurl needs the jonerix CA bundle path; jpkg build runs in a
# non-login shell so profile.d isn't auto-sourced).
build = """export CARGO_HTTP_CAINFO=/etc/ssl/certs/ca-certificates.crt
export OPENSSL_DIR=/
export OPENSSL_INCLUDE_DIR=/include
export OPENSSL_LIB_DIR=/lib
cargo build --release"""
install = """install -Dm755 target/release/jmake "$DESTDIR/bin/jmake"
llvm-strip --strip-all "$DESTDIR/bin/jmake" 2>/dev/null || true"""

[depends]
runtime = ["musl"]
build = ["rust"]
